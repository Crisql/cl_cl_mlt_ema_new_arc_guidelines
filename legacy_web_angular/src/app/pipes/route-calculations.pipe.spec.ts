import { RouteCalculationsPipe } from './route-calculations.pipe';

describe('RouteCalculationsPipe', () => {
  it('create an instance', () => {
    const pipe = new RouteCalculationsPipe();
    expect(pipe).toBeTruthy();
  });
});
