import { SyncDocumentStatusNamePipe } from './sync-document-status-name.pipe';

describe('SyncDocumentStatusNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SyncDocumentStatusNamePipe();
    expect(pipe).toBeTruthy();
  });
});
